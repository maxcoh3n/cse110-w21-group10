const functionAdd = {
  add: (num1, num2) => num1 + num2
};

const functionSub = {
  subtract: (num1, num2) => num1 - num2
};

module.exports = {
  functionAdd : functionAdd,
  functionSub : functionSub
};